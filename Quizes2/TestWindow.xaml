<Window x:Class="Quizes2.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quizes2"
        mc:Ignorable="d"
        Title="Тест" Width="600" Height="500">
    <Window.Background>
        <ImageBrush ImageSource="/фон.png" Stretch="None" TileMode="Tile"/>
    </Window.Background>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Прогресс -->
        <TextBlock x:Name="ProgressText"
                   FontSize="14" 
                   Margin="0 0 0 10"
                   Grid.Row="0" FontFamily="Cascadia Mono"/>

        <!-- Вопрос -->
        <TextBlock x:Name="QuestionText"
                   FontSize="18" 
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Grid.Row="1" FontFamily="Cascadia Mono"/>

        <!-- Ответы с ScrollViewer -->
        <ScrollViewer Grid.Row="2" 
                      Margin="0 20 0 20"
                      VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono">
            <StackPanel x:Name="AnswersPanel" />
        </ScrollViewer>

        <!-- Кнопки навигации -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <DockPanel>
                <Button x:Name="BackButton" 
                        Content="Назад" 
                        Width="120" 
                        Height="40"
                        Margin="0,0,10,0"
                        Click="BackButton_Click"
                        Visibility="Hidden" Background="#FFE5EBFF" BorderBrush="#FFB5B5B5" FontFamily="Cascadia Mono"/>
                <Button x:Name="NextButton"
                        Content="Далее"
                        Width="120" 
                        Height="40"
                        Click="NextButton_Click" Background="#FFE5EBFF" BorderBrush="#FFB5B5B5" FontFamily="Cascadia Mono"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>
